[package]
name = "cashback_program"
version = "0.1.0"
description = "PRISM cashback program — treasury-funded SOL refunds"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "cashback_program"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }
